spring.config.import=file:env.properties
logging.level.root=${LOG_LEVEL:WARN}
logging.file.name=${LOG_FILE_NAME:C:/Users/sarjbtom/repositories/storeapplication-ecommerce/backend/logs/app-qa.log}
logging.pattern.file={"timestamp":"%d{yyyy-MM-dd HH:mm:ss}","level":"%p","logger":"%c","message":"%m"}

spring.datasource.url=${DATABASE_URL}/${DATABASE}
spring.datasource.username=${USER}
spring.datasource.password=${PASSWORD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

jwt.secret=${JWT_SECRET}
jwt.expiration-time-ms=${JWT_EXPIRATION_MS}
stripe.apiKey=${STRIPE_API_KEY}
contact.phone=${CONTACT_PHONE_QA}
contact.email=${CONTACT_EMAIL_QA}
contact.address=${CONTACT_ADDRESS_QA}

management.endpoint.health.show-details=always
management.endpoints.web.base-path=/backend/actuator
management.endpoints.web.exposure.include=*